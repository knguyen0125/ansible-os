---
- name: install software
  hosts: localhost
  remote_user: kevin
  vars_files:
    - ./variables.yaml
  vars:
    system_packages:
      - albert
      - solaar
      - tilix
      - vlc
      - peek
      - gifski
      - ibus
      - ibus-unikey
      - neovim
      - yay
      - base-devel
    aur_packages:
      - google-chrome
      - antibody
    flatpak_packages:
      - com.spotify.Client
      - com.slack.Slack
  roles:
    - kewlfft.aur
    - users
    - common
    - aws
    - cloud
    - ide
    - docker
    - python
    - node
    - zsh
    - gnome
    # - role: jaredhocutt.gnome_extensions
    #   vars:
    #     gnome_extension_ids:
    #     - 1723 # ShellTile
