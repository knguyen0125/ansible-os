- name: Create 'aur_builder' user
  user:
    name: aur_builder
  become: true
- name: Allow 'aur_builder' to install packages
  lineinfile:
    path: /etc/sudoers.d/11-install-aur_builder
    line: 'aur_builder ALL=(ALL) NOPASSWD: /usr/bin/pacman'
    create: yes
    validate: 'visudo -cf %s'
  become: true